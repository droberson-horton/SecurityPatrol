apiVersion: v1
kind: Service
metadata:
  name: security-patrol-app
  namespace: security-patrol
  labels:
    app: security-patrol-app
spec:
  type: ClusterIP
  selector:
    app: security-patrol-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
